/* globals.js */
/**
 * Definiert globale Variablen, die in verschiedenen Funktionen verwendet werden.
 * - textFields: Array, das alle Textfeld-Objekte speichert (inklusive Eingabeelemente und Overlays).
 * - memeCanvas: Referenz auf das Canvas-Element, in dem das Meme gezeichnet wird.
 * - ctx: Zeichenkontext des Canvas.
 * - img: Das Bild, das als Hintergrund für das Meme dient.
 */
let textFields = [];
let memeCanvas, ctx, img;

